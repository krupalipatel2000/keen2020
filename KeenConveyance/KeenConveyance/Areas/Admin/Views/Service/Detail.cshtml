@model KeenConveyance.Areas.Admin.Models.tblService
@{
    ViewBag.Title = "Detail";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
<div>
    <div class="row">
        <div class="col-md-4" style="text-align:center">
            <img src="~/Images/@Html.DisplayFor(model => model.ServiceImage)" class="img-thumbnail" />
            <p>
                Member since <b> @Html.ViewBag.Year </b>
            </p>
        </div>
        <div class="col-md-8">
            <div class="row">
                <h1 class="alert-light">@Html.DisplayFor(model => model.CompanyId)'s Service</h1>
            </div>
            <div class="row">
                <h4>ServiceName : @Html.DisplayFor(model => model.ServiceName)</h4>
            </div>
            <div class="row">
                <h4>ServiceDesc : @Html.DisplayFor(model => model.ServiceDesc)</h4>
            </div>
            <div class="row">
                <h4>
                    IsActive : @if (Model.IsActive == true)
                    {
                        <div class="custom-control custom-switch">
                            <input type="checkbox" disabled checked class="custom-control-input" id="active_@Model.ServiceId" onchange="return activate(@Model.ServiceId)">
                            <label class="custom-control-label" for="active_@Model.ServiceId"></label>
                        </div>
                    }
                    else
                    {
                        <div class="custom-control custom-switch">
                            <input type="checkbox" disabled class="custom-control-input" id="active_@Model.ServiceId" onchange="return activate(@Model.ServiceId)">
                            <label class="custom-control-label" for="active_@Model.ServiceId"></label>
                        </div>
                    }
                </h4>
            </div>
            <div class="row">
                <h4>CreatedOn : @Html.DisplayFor(model => model.CreatedOn)</h4>
            </div>
        </div>
    </div>
</div>
@*<script>
        function activate(id) {
            $.ajax(
                {
                    type: "POST",
                    url: "@Url.Action("Active","Company")" + "/" + id,
                    success: function (response) {
                        if (response == true) {
                            alert("Account activated");
                        }
                        else {
                            alert("Accout deactivated");
                        }
                    },
                    failure: function (response) {
                        alert('Fail - ' + response.responseText);
                    },
                    error: function (response) {
                        alert('Error - ' + response.responseText);
                    }
                });
        }
    </script>*@


