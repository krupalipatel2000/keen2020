
@{
    ViewBag.Title = "Insert2";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<script>
    function validate() {
        var budget = document.registration.txtBudget;
        var minbid = document.registration.txtminbidvalue;
        var maxbid = document.registration.txtmaxbidvalue;
        var mintime = document.registration.txtmintime;
        var maxtime = document.registration.txtmaxtime;
        var rname = document.registration.txtName;
        var cno = document.registration.txtCno;
        var pin = document.registration.txtPin;
     

        if (budget.value != "" && budget.value >= @ViewBag.Budget ) {
        }
        else {
            alert("Budget is not Proper");
            budget.focus();
            return false;
        }
        if (minbid.value != "") {
        }
        else {
            alert("MinBidvalue is not proper");
            minbid.focus();
            return false;
        }
        if (maxbid.value != "") {
        }
        else {
            alert("MinBidvalue is not proper");
            maxbid.focus();
            return false;
        }
        if (mintime.value != "") {
        }
        else {
            alert("MinTime not proper");
            mintime.focus();
            return false;
        }
        if (maxtime.value != "") {
        }
        else {
            alert("MaxTime is not proper");
            maxtime.focus();
            return false;
        }
        var rname_pat = /^[A-Za-z]+$/;
        if (rname.value != "" && rname.value.match(rname_pat)) {
        }
        else {
            alert("Receiver Name is not proper");
            rname.focus();
            return false;
        }
        var cno_pat = /^\d{10}$/;
        if (cno.value != "" && cno.value.match(cno_pat)) {
        }
        else {
            alert("ContactNo is not proper");
            cno.focus();
            return false;
        }
        var pin_pat = /^\d{4}$/;
        if (pin.value != "" && pin.value.match(pin_pat)) {
        }
        else {
            alert("PinNo is not proper");
            pin.focus();
            return false;
        }
        return true;
    }
</script>


<center>
    <div class="p-2">
        <div class="card shadow mb-4" style="width:1000px;height:500px;">
            <div class="card-header py-3">
                <center><h4 class="m-0 font-weight-bold text-primary">Add Consignment</h4></center>
            </div>
            <div class="card-body">
                <form class="user" method="post" name="registration" enctype="multipart/form-data">
                    <div class="form-group row">
                        <div class="col-sm-6 mb-3 mb-sm-0">
                            Your budget is : @ViewBag.Budget
                        </div>
                        <div class="col-sm-6 mb-3 mb-sm-0">
                            <input type="text" class="form-control" name="txtBudget" placeholder="Budget">
                        </div>
                    </div>
                    <div class="form-group row">
                        <div class="col-sm-6 mb-3 mb-sm-0">
                            <input type="number" class="form-control" name="txtminbidvalue" placeholder="minbidvalue" min="1">
                        </div>
                        <div class="col-sm-6 mb-3 mb-sm-0">
                            <input type="number" class="form-control" name="txtmaxbidvalue" placeholder="maxbidvalue" min="1">
                        </div>
                    </div>
                    <div class="form-group row">
                        <div class="col-sm-6 mb-3 mb-sm-0">
                            <input type="text" class="form-control" name="txtmintime" placeholder="mintime">
                        </div>
                        <div class="col-sm-6 mb-3 mb-sm-0">
                            <input type="text" class="form-control" name="txtmaxtime" placeholder="maxtime">
                        </div>
                    </div>
                    <div class="form-group row">
                        <div class="col-sm-6 mb-3 mb-sm-0">
                            <input type="text" class="form-control" name="txtName" placeholder="Receiver's Name">
                        </div>
                        <div class="col-sm-6 mb-3 mb-sm-0">
                            <input type="text" class="form-control" name="txtCno" placeholder="Receiver's ContactNo">
                        </div>
                    </div>
                    <div class="form-group row">
                        <div class="col-sm-6 mb-3 mb-sm-0">
                            <input type="text" class="form-control" name="txtPin" placeholder="Receiver's Pin Number">
                        </div>

                    </div>
                    <br /><br /><br />
                    <div style="overflow:auto;">
                        <div style="float:right;">
                            <button type="submit" class="btn btn-primary" id="nextBtn" onclick="return validate();">Next</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</center>
