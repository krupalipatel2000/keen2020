@{
    var myChart = new Chart(width: 600, height: 400, theme: ChartTheme.Green)

        .AddTitle("Monthwise User Registration")
        .AddSeries(
            name: "Monthwise User",
            xValue: ViewBag.X,
            yValues: ViewBag.Y).Write();
}

